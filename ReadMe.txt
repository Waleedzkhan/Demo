Introduction:

The project contains three components

1. A local database named DABUNI
2. An Asp.net webform application to display data
3. an Asmx webservice which is used by Jquery to fetch and display data in
   the webform.

The application has a small translation feature which can be used by
pressing the Ar or EN links on the top of the page.
